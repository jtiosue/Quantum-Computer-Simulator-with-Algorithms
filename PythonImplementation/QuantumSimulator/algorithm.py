#TODO

def canonical_ordering(alg):
    """
    alg is a list of strings corresponding to gates
    return a list of the algorithm ordered in the desired manner

    ie, alg = ["H(1)", "CN(0, 1)", "Y(0)", "X(1)", ...]
    """
    return alg